{
  "name": "korutla-town",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "publish:build": "npm run build && echo \"\\n✓ Ready. Upload the dist/ folder (see PUBLISH.md).\""
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
