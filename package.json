{
  "name": "korutla-town",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "publish:build": "npm run build && echo \"\\n✓ dist/ ready for deploy.\""
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
